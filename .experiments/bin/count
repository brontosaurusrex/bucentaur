#!/bin/zsh

# count

# A simle countdown ZSH script
# Usage: count 20 # in seconds
# Note: This is NOT miliseconds precise.

c="$1" && d="$c"
echo -ne "${(l(3)( ))c}\r"
repeat c {(( c-- )); sleep 1; echo -ne "${(l(3)( ))c}\r"} && echo "$d seconds has passed."
